diff a/src/components/ui/blur-stack.tsx b/src/components/ui/blur-stack.tsx	(rejected hunks)
@@ -2,3 +2,3 @@ export interface BlurLayer {
-  blur: number;
-  maskStops: [number, number, number, number]; // [fadeInStart, solidStart, solidEnd, fadeOutEnd]
-  zIndex: number;
+	blur: number;
+	maskStops: [number, number, number, number]; // [fadeInStart, solidStart, solidEnd, fadeOutEnd]
+	zIndex: number;
@@ -8,8 +8,8 @@ const DEFAULT_BLUR_CONFIG: BlurLayer[] = [
-  { blur: 0.5, maskStops: [0, 12.5, 25, 37.5], zIndex: 1 },
-  { blur: 1, maskStops: [12.5, 25, 37.5, 50], zIndex: 2 },
-  { blur: 2, maskStops: [25, 37.5, 50, 62.5], zIndex: 3 },
-  { blur: 4, maskStops: [37.5, 50, 62.5, 75], zIndex: 4 },
-  { blur: 8, maskStops: [50, 62.5, 75, 87.5], zIndex: 5 },
-  { blur: 16, maskStops: [62.5, 75, 87.5, 100], zIndex: 6 },
-  { blur: 32, maskStops: [75, 87.5, 100, 100], zIndex: 7 },
-  { blur: 64, maskStops: [87.5, 100, 100, 100], zIndex: 8 },
+	{ blur: 0.5, maskStops: [0, 12.5, 25, 37.5], zIndex: 1 },
+	{ blur: 1, maskStops: [12.5, 25, 37.5, 50], zIndex: 2 },
+	{ blur: 2, maskStops: [25, 37.5, 50, 62.5], zIndex: 3 },
+	{ blur: 4, maskStops: [37.5, 50, 62.5, 75], zIndex: 4 },
+	{ blur: 8, maskStops: [50, 62.5, 75, 87.5], zIndex: 5 },
+	{ blur: 16, maskStops: [62.5, 75, 87.5, 100], zIndex: 6 },
+	{ blur: 32, maskStops: [75, 87.5, 100, 100], zIndex: 7 },
+	{ blur: 64, maskStops: [87.5, 100, 100, 100], zIndex: 8 },
@@ -19,2 +19,2 @@ export default function BlurStack({
-  className,
-  config = DEFAULT_BLUR_CONFIG,
+	className,
+	config = DEFAULT_BLUR_CONFIG,
@@ -22,2 +22,2 @@ export default function BlurStack({
-  className?: string;
-  config?: BlurLayer[];
+	className?: string;
+	config?: BlurLayer[];
@@ -25,6 +25,6 @@ export default function BlurStack({
-  return (
-    <div className={className}>
-      <div className="absolute inset-0 overflow-hidden">
-        {config.map((layer, index) => {
-          const [start, solidStart, solidEnd, end] = layer.maskStops;
-          const maskImage = `linear-gradient(rgba(0,0,0,0) ${start}%, rgb(0,0,0) ${solidStart}%, rgb(0,0,0) ${solidEnd}%, rgba(0,0,0,0) ${end}%)`;
+	return (
+		<div className={className}>
+			<div className="absolute inset-0 overflow-hidden">
+				{config.map((layer, index) => {
+					const [start, solidStart, solidEnd, end] = layer.maskStops;
+					const _maskImage = `linear-gradient(rgba(0,0,0,0) ${start}%, rgb(0,0,0) ${solidStart}%, rgb(0,0,0) ${solidEnd}%, rgba(0,0,0,0) ${end}%)`;
@@ -32,16 +32,6 @@ export default function BlurStack({
-          return (
-            <div
-              key={index}
-              className="absolute inset-0 pointer-events-none"
-              style={{
-                zIndex: layer.zIndex,
-                maskImage,
-                backdropFilter: `blur(${layer.blur}px)`,
-                WebkitBackdropFilter: `blur(${layer.blur}px)`,
-              }}
-            />
-          );
-        })}
-      </div>
-    </div>
-  );
+					// biome-ignore lint/suspicious/noArrayIndexKey: static config array, order never changes
+					return <div key={index} className="absolute inset-0 pointer-events-none" />;
+				})}
+			</div>
+		</div>
+	);
