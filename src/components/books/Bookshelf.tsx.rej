diff a/src/components/books/Bookshelf.tsx b/src/components/books/Bookshelf.tsx	(rejected hunks)
@@ -8,11 +8,11 @@ interface Book {
-  slug: string;
-  title: string;
-  author: string;
-  cover?: string;
-  status: string;
-  rating?: number;
-  review?: string;
-  genre: string[];
-  year?: number;
-  pages?: number;
-  dateRead?: string;
+	slug: string;
+	title: string;
+	author: string;
+	cover?: string;
+	status: string;
+	rating?: number;
+	review?: string;
+	genre: string[];
+	year?: number;
+	pages?: number;
+	dateRead?: string;
@@ -22 +22 @@ function Shelf({ books, label }: { books: Book[]; label: string }) {
-  const [selected, setSelected] = useState<Book | null>(null);
+	const [selected, setSelected] = useState<Book | null>(null);
@@ -24 +24 @@ function Shelf({ books, label }: { books: Book[]; label: string }) {
-  if (books.length === 0) return null;
+	if (books.length === 0) return null;
@@ -26,3 +26,3 @@ function Shelf({ books, label }: { books: Book[]; label: string }) {
-  return (
-    <div className="mb-10">
-      <div className="text-label mb-4 text-[var(--muted-foreground)]">{label}</div>
+	return (
+		<div className="mb-10">
+			<div className="text-label mb-4 text-[var(--muted-foreground)]">{label}</div>
@@ -30 +30 @@ function Shelf({ books, label }: { books: Book[]; label: string }) {
-      {/*
+			{/*
@@ -34 +34,3 @@ function Shelf({ books, label }: { books: Book[]; label: string }) {
-      <div style={{ position: 'relative' }}>
+			<div className="relative">
+				{/* ── SHELF CASING OUTLINE (drawn with box-shadow, not overflow:hidden) ── */}
+				<div className="absolute inset-0 rounded-[10px] bg-[linear-gradient(180deg,#5c3a1e_0%,#3e2510_100%)] z-0" />
@@ -36,19 +38,2 @@ function Shelf({ books, label }: { books: Book[]; label: string }) {
-        {/* ── SHELF CASING OUTLINE (drawn with box-shadow, not overflow:hidden) ── */}
-        <div
-          style={{
-            position: 'absolute',
-            inset: 0,
-            borderRadius: 10,
-            // Outer walnut casing via box-shadow + gradient border trick
-            background: 'linear-gradient(180deg, #5c3a1e 0%, #3e2510 100%)',
-            zIndex: 0,
-          }}
-        />
-        {/* Casing top highlight strip */}
-        <div style={{
-          position: 'absolute',
-          top: 0, left: 0, right: 0, height: 3,
-          background: 'linear-gradient(90deg, #8b6040 0%, #c09060 40%, #a07040 70%, #7a5030 100%)',
-          borderRadius: '10px 10px 0 0',
-          zIndex: 1,
-        }} />
+				{/* Casing top highlight strip */}
+				<div className="absolute top-0 left-0 right-0 h-[3px] bg-[linear-gradient(90deg,#8b6040_0%,#c09060_40%,#a07040_70%,#7a5030_100%)] rounded-t-[10px] z-[1]" />
@@ -56,35 +41,24 @@ function Shelf({ books, label }: { books: Book[]; label: string }) {
-        {/* ── BACK WALL (absolute, fills behind books) ── */}
-        <div
-          style={{
-            position: 'absolute',
-            inset: 3,
-            borderRadius: 8,
-            // Dark walnut with grain
-            background: [
-              'repeating-linear-gradient(180deg, transparent 0px, transparent 9px, rgba(0,0,0,0.055) 9px, rgba(0,0,0,0.055) 10px)',
-              'repeating-linear-gradient(182deg, transparent 0px, transparent 45px, rgba(255,255,255,0.01) 45px, rgba(255,255,255,0.01) 46px)',
-              'linear-gradient(180deg, #2c1c0e 0%, #1a1008 100%)',
-            ].join(', '),
-            zIndex: 1,
-            overflow: 'hidden',
-          }}
-        >
-          {/* Overhead warm light cone */}
-          <div style={{
-            position: 'absolute', inset: 0,
-            background: 'radial-gradient(ellipse 65% 50% at 50% -5%, rgba(255,195,100,0.16) 0%, rgba(255,160,60,0.05) 55%, transparent 80%)',
-            pointerEvents: 'none',
-          }} />
-          {/* Left vignette */}
-          <div style={{
-            position: 'absolute', top: 0, left: 0, width: 48, bottom: 0,
-            background: 'linear-gradient(90deg, rgba(0,0,0,0.28) 0%, transparent 100%)',
-            pointerEvents: 'none',
-          }} />
-          {/* Right vignette */}
-          <div style={{
-            position: 'absolute', top: 0, right: 0, width: 48, bottom: 0,
-            background: 'linear-gradient(270deg, rgba(0,0,0,0.28) 0%, transparent 100%)',
-            pointerEvents: 'none',
-          }} />
-        </div>
+				{/* ── BACK WALL (absolute, fills behind books) ── */}
+				<div
+					// biome-ignore lint/nursery/noInlineStyles: complex multi-stop grain pattern not expressible as a single className
+					style={{
+						position: 'absolute',
+						inset: 3,
+						borderRadius: 8,
+						// Dark walnut with grain
+						background: [
+							'repeating-linear-gradient(180deg, transparent 0px, transparent 9px, rgba(0,0,0,0.055) 9px, rgba(0,0,0,0.055) 10px)',
+							'repeating-linear-gradient(182deg, transparent 0px, transparent 45px, rgba(255,255,255,0.01) 45px, rgba(255,255,255,0.01) 46px)',
+							'linear-gradient(180deg, #2c1c0e 0%, #1a1008 100%)',
+						].join(', '),
+						zIndex: 1,
+						overflow: 'hidden',
+					}}
+				>
+					{/* Overhead warm light cone */}
+					<div className="absolute inset-0 bg-[radial-gradient(ellipse_65%_50%_at_50%_-5%,rgba(255,195,100,0.16)_0%,rgba(255,160,60,0.05)_55%,transparent_80%)] pointer-events-none" />
+					{/* Left vignette */}
+					<div className="absolute inset-y-0 left-0 w-12 bg-[linear-gradient(90deg,rgba(0,0,0,0.28)_0%,transparent_100%)] pointer-events-none" />
+					{/* Right vignette */}
+					<div className="absolute inset-y-0 right-0 w-12 bg-[linear-gradient(270deg,rgba(0,0,0,0.28)_0%,transparent_100%)] pointer-events-none" />
+				</div>
@@ -92,30 +66,31 @@ function Shelf({ books, label }: { books: Book[]; label: string }) {
-        {/* ── BOOKS ROW — perspective set here, overflow VISIBLE so books can pop out ── */}
-        <div
-          className="flex overflow-x-auto"
-          style={{
-            position: 'relative',
-            zIndex: 3,
-            padding: '20px 20px 0 20px',
-            alignItems: 'flex-end',
-            gap: 2,
-            // Perspective on the ROW so all books share the same 3D space
-            perspective: '1000px',
-            perspectiveOrigin: '50% 110%',
-            // MUST be visible — this is what lets books tilt out of the shelf
-            overflow: 'visible',
-            scrollbarWidth: 'thin',
-            scrollbarColor: 'rgba(255,255,255,0.08) transparent',
-          }}
-        >
-          {books.map((book, i) => (
-            <Book3D
-              key={book.slug}
-              title={book.title}
-              author={book.author}
-              cover={book.cover}
-              pages={book.pages}
-              index={i}
-              onClick={() => setSelected(book)}
-            />
-          ))}
-        </div>
+				{/* ── BOOKS ROW — perspective set here, overflow VISIBLE so books can pop out ── */}
+				<div
+					className="flex overflow-x-auto"
+					// biome-ignore lint/nursery/noInlineStyles: perspective/perspectiveOrigin/scrollbarColor have no Tailwind equivalent; overflow:visible intentionally overrides className
+					style={{
+						position: 'relative',
+						zIndex: 3,
+						padding: '20px 20px 0 20px',
+						alignItems: 'flex-end',
+						gap: 2,
+						// Perspective on the ROW so all books share the same 3D space
+						perspective: '1000px',
+						perspectiveOrigin: '50% 110%',
+						// MUST be visible — this is what lets books tilt out of the shelf
+						overflow: 'visible',
+						scrollbarWidth: 'thin',
+						scrollbarColor: 'rgba(255,255,255,0.08) transparent',
+					}}
+				>
+					{books.map((book, i) => (
+						<Book3D
+							key={book.slug}
+							title={book.title}
+							author={book.author}
+							cover={book.cover}
+							pages={book.pages}
+							index={i}
+							onClick={() => setSelected(book)}
+						/>
+					))}
+				</div>
@@ -123,45 +98,17 @@ function Shelf({ books, label }: { books: Book[]; label: string }) {
-        {/* ── SHELF PLANK (sits above books in z so shadow falls correctly) ── */}
-        <div style={{ position: 'relative', zIndex: 4 }}>
-          {/* Plank top — catches overhead light */}
-          <div style={{
-            height: 7,
-            background: 'linear-gradient(180deg, #a07540 0%, #7a5428 55%, #5c3c18 100%)',
-            position: 'relative',
-            overflow: 'hidden',
-            marginLeft: 3, marginRight: 3,
-          }}>
-            <div style={{
-              position: 'absolute', inset: 0,
-              backgroundImage: 'repeating-linear-gradient(90deg, transparent 0px, transparent 16px, rgba(0,0,0,0.06) 16px, rgba(0,0,0,0.06) 17px)',
-            }} />
-            {/* Light glint */}
-            <div style={{
-              position: 'absolute', top: 1, left: '15%', right: '15%', height: 1,
-              background: 'rgba(255,230,170,0.4)',
-            }} />
-          </div>
-          {/* Plank front face */}
-          <div style={{
-            height: 20,
-            background: 'linear-gradient(180deg, #6b4820 0%, #4e3210 55%, #3a2408 100%)',
-            position: 'relative',
-            overflow: 'hidden',
-            marginLeft: 3, marginRight: 3,
-            borderRadius: '0 0 7px 7px',
-          }}>
-            <div style={{
-              position: 'absolute', inset: 0,
-              backgroundImage: 'repeating-linear-gradient(90deg, transparent 0px, transparent 22px, rgba(0,0,0,0.07) 22px, rgba(0,0,0,0.07) 23px)',
-            }} />
-            <div style={{
-              position: 'absolute', top: 0, left: 0, right: 0, height: 1,
-              background: 'rgba(255,200,120,0.18)',
-            }} />
-          </div>
-          {/* Underside shadow drip */}
-          <div style={{
-            height: 10,
-            background: 'linear-gradient(180deg, rgba(0,0,0,0.5) 0%, rgba(0,0,0,0) 100%)',
-            marginLeft: 3, marginRight: 3,
-          }} />
-        </div>
+				{/* ── SHELF PLANK (sits above books in z so shadow falls correctly) ── */}
+				<div className="relative z-[4]">
+					{/* Plank top — catches overhead light */}
+					<div className="h-[7px] bg-[linear-gradient(180deg,#a07540_0%,#7a5428_55%,#5c3c18_100%)] relative overflow-hidden mx-[3px]">
+						<div className="absolute inset-0 bg-[image:repeating-linear-gradient(90deg,transparent_0px,transparent_16px,rgba(0,0,0,0.06)_16px,rgba(0,0,0,0.06)_17px)]" />
+						{/* Light glint */}
+						<div className="absolute top-[1px] left-[15%] right-[15%] h-px bg-[rgba(255,230,170,0.4)]" />
+					</div>
+					{/* Plank front face */}
+					<div className="h-5 bg-[linear-gradient(180deg,#6b4820_0%,#4e3210_55%,#3a2408_100%)] relative overflow-hidden mx-[3px] rounded-b-[7px]">
+						<div className="absolute inset-0 bg-[image:repeating-linear-gradient(90deg,transparent_0px,transparent_22px,rgba(0,0,0,0.07)_22px,rgba(0,0,0,0.07)_23px)]" />
+						<div className="absolute top-0 inset-x-0 h-px bg-[rgba(255,200,120,0.18)]" />
+					</div>
+					{/* Underside shadow drip */}
+					<div className="h-[10px] bg-[linear-gradient(180deg,rgba(0,0,0,0.5)_0%,rgba(0,0,0,0)_100%)] mx-[3px]" />
+				</div>
+			</div>
@@ -169,9 +116,3 @@ function Shelf({ books, label }: { books: Book[]; label: string }) {
-      </div>
-
-      <BookDetail
-        book={selected}
-        open={!!selected}
-        onClose={() => setSelected(null)}
-      />
-    </div>
-  );
+			<BookDetail book={selected} open={!!selected} onClose={() => setSelected(null)} />
+		</div>
+	);
@@ -181,3 +122,3 @@ export default function Bookshelf({ books }: { books: Book[] }) {
-  const readBooks = books.filter(b => b.status === 'read');
-  const readingBooks = books.filter(b => b.status === 'reading');
-  const toReadBooks = books.filter(b => b.status === 'to-read');
+	const readBooks = books.filter((b) => b.status === 'read');
+	const readingBooks = books.filter((b) => b.status === 'reading');
+	const toReadBooks = books.filter((b) => b.status === 'to-read');
@@ -185,9 +126,7 @@ export default function Bookshelf({ books }: { books: Book[] }) {
-  return (
-    <div>
-      {readingBooks.length > 0 && (
-        <Shelf books={readingBooks} label="Currently Reading" />
-      )}
-      <Shelf books={readBooks} label="Have Read" />
-      <Shelf books={toReadBooks} label="Want to Read" />
-    </div>
-  );
+	return (
+		<div>
+			{readingBooks.length > 0 && <Shelf books={readingBooks} label="Currently Reading" />}
+			<Shelf books={readBooks} label="Have Read" />
+			<Shelf books={toReadBooks} label="Want to Read" />
+		</div>
+	);
