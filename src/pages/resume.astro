---
import { Briefcase01Icon, HugeiconsIcon } from '@icons';
import ExperienceList from '@/components/resume/ExperienceList.astro';
import ButtonLink from '@/components/ui/ButtonLink';
import { education } from '@/data/experience';
import PageLayout from '@/layouts/PageLayout.astro';
---

<PageLayout
  title="Resume"
  description="Aryan Randeriya, developer and designer with a focus on full-stack web and mobile development."
>
  <div class="pb-16 print:pb-0">

    <!-- ── Header ───────────────────────────────────────────────── -->
    <div class="mb-3 pb-8">
      <h1
        class="text-heading-1 tracking-[-0.03em] mb-1.5 animate-fade-in stagger-2"
      >
        Professional Experience
      </h1>

      <p
        class="text-sm text-[var(--text-muted)] mb-2.5 animate-fade-in stagger-3"
      >
        Developer · Designer · Builder
      </p>


<div class="mt-5 overflow-hidden print:hidden">
        <ButtonLink
          href="/resume.pdf"
          label="Download PDF"
          icon="download"
          className="rounded-xl"
          iconSize={13}
          size="sm"
          variant="accent"
        />
      </div>
    </div>

    <!-- ── Experience ────────────────────────────────────────────── -->
    <section class="mb-5">
      <p class="section-header animate-fade-in" style="animation-delay:0.05s">Experience</p>
      <ExperienceList excludeTypes={["volunteer"]} />
    </section>

    <!-- ── Community Experience ──────────────────────────────────── -->
    <section class="mb-5">
      <p class="section-header animate-fade-in" style="animation-delay:0.05s">Community</p>
      <ExperienceList includeTypes={["volunteer"]} />
    </section>

    <!-- ── Education ─────────────────────────────────────────────── -->
    <section class="mb-10">
      <p class="section-header animate-fade-in" style="animation-delay:0.05s">Education</p>
      <div class="flex flex-col">
        {education.map((edu, i) => (
          <div class:list={[
            'flex gap-3 items-start py-3.5',
            i < education.length - 1 && 'border-b border-[var(--border)]',
          ]}>
            <div class="pt-px shrink-0">
              <img
                src={edu.logo}
                alt={edu.institution}
                width="28"
                height="28"
                class="w-7 h-7 rounded-md object-contain"
                onerror="this.style.display='none';this.nextElementSibling.style.display='flex';"
              />
              <div class="hidden w-7 h-7 rounded-md items-center justify-center text-[var(--text-ghost)]">
                <HugeiconsIcon icon={Briefcase01Icon} size={16} />
              </div>
            </div>
            <div class="flex-1 min-w-0">
              <div class="flex items-baseline gap-2 flex-wrap">
                <span class="text-sm font-semibold text-[var(--text-secondary)] tracking-[-0.01em]">
                  {edu.shortName}
                </span>
                <span class="text-xs text-[var(--text-muted)]">
                  {edu.degree} · {edu.field}
                </span>
                <span class="text-[11px] text-[var(--text-ghost)] ml-auto whitespace-nowrap">
                  {edu.startYear}–{edu.endYear}
                </span>
              </div>
              <div class="text-[11px] text-[var(--text-ghost)] mt-0.5">
                {edu.institution} · {edu.location}
              </div>
            </div>
          </div>
        ))}
      </div>
    </section>

  </div>
</PageLayout>
