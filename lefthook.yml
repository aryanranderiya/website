pre-commit:
  parallel: true
  commands:
    biome:
      glob: "*.{ts,tsx,astro,json}"
      # Safe fixes only — --unsafe would destructively delete style props (noInlineStyles)
      run: pnpm biome check --write {staged_files}
      stage_fixed: true
    type-check:
      run: pnpm astro check

pre-push:
  parallel: true
  commands:
    biome:
      run: pnpm biome check ./src
    type-check:
      run: pnpm astro check
